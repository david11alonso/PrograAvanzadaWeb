@model IEnumerable<FrontEnd.Models.PropuestaDepartamento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Departamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Propuesta)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Departamento.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Propuesta.Beneficios)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PropuestaDepartamentoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PropuestaDepartamentoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PropuestaDepartamentoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
